services:
  - type: web
    name: youtube-proxy
    runtime: node
    buildCommand: >
      apt-get update &&
      apt-get install -y python3 python3-pip &&
      pip install -r requirements.txt &&
      npm install
    startCommand: node server3.js
    envVars:
      - key: PORT
        value: 3000
